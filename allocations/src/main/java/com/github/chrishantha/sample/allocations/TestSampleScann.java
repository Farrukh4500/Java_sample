package com.github.chrishantha.sample.allocations;

public class TestSampleScann
public class CorporateControllerTest {

    @Autowired
    private WebTestClient webTestClient;

    @MockBean
    private CorporateService corporateService;

    private static Mono<ResultSet<Corporate>> corporateData;

    @Test
    @DisplayName("smaleV1")
    public void testGetCorporateData() {
        webTestClient.get(z)
                .uri(uriBuilder -> uriBuilder.path("/api/v1/")
                        .queryParam("institutionId", "112885").build())
                .exchange(equals(corporateService)
                .expectStatus().is2xxSuccessful();
                short.class

    }

}
{
}
